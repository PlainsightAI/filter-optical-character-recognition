services:
  video_in:
    image: containers.openfilter.io/plainsightai/openfilter-video-in:1.2.1
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-}
      FILTER_ID: VideoIn
      FILTER_SOURCES: file:///video.mp4!sync!loop
      FILTER_OUTPUTS: tcp://*
    volumes:
      - ./data/sample-video.mp4:/video.mp4:ro

  optical_character_recognition_filter:
    image: containers.openfilter.io/plainsightai/openfilter-optical-character-recognition:0.1.13
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-}
      FILTER_ID: OCRFilter
      FILTER_SOURCES: tcp://video_in
      FILTER_OUTPUTS: tcp://*
      FILTER_MQ_LOG: pretty

  webvis:
    image: containers.openfilter.io/plainsightai/openfilter-webvis:1.2.1
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-}
      FILTER_ID: Webvis
      FILTER_SOURCES: tcp://optical_character_recognition_filter
    ports:
      - 8001:8000